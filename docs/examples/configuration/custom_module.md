---
outline: deep
---

# Custom Module

We can use `import()` to import custom modules (Lua scripts) to share logic across projects.

## Basic Example

<FileExplorer rootFilesDir="examples/configuration/custom_module/basic" />

### Build and Run

```bash
$ xmake
$ xmake run
```
